# Executables produced by build.sh / build.bat / go build
Rush-Hour
Rush-Hour.exe
rush-hour

# The environment server. Anchored: an unanchored "rushhour-env" would also
# match the cmd/rushhour-env source directory.
/rushhour-env
/rushhour-env.exe

# Build/packaging output
dist/

# WebAssembly bundle (built by the Pages workflow / wasmsdl)
_build/
dist-wasm/

# Data files, if they ever land here rather than in $HOME/goxpy_data
goxpy_data/

.DS_Store
